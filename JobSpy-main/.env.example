# JobSpy – copy to .env and edit. example.py uses these via config.py.
JOBSPY_SEARCH_TERM=software engineer
JOBSPY_LOCATION=San Francisco, CA
JOBSPY_COUNTRY=USA
JOBSPY_RESULTS_WANTED=10
JOBSPY_VERBOSE=2
# Comma-separated: indeed, linkedin, zip_recruiter, glassdoor, google, bayt, naukri, bdjobs
JOBSPY_SITES=indeed
# Optional: only jobs posted in last N hours (e.g. 24, 72, 168). Leave empty for no date filter (may include older/expired).
# JOBSPY_HOURS_OLD=72
# Optional: comma-separated proxies (host:port or user:pass@host:port)
# JOBSPY_PROXIES=
